class Solution {
    public int scoreOfString(String s) {
        int ch=0;
        int t=0;
        for(int i=0;i<s.length()-1;i++){
             ch=Math.abs((int) s.charAt(i)-(int)s.charAt(i+1));
              t=t+ch;
           
        }
        return t;
    }
}
